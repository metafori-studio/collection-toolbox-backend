#!/usr/bin/env bash
name=$1
url=$2

echo -n "Waiting for $name to be ready..."
count=0
until curl -s "$url" >/dev/null 2>&1; do
    sleep 1
    count=$((count + 1))
    if [ $count -ge 30 ]; then
        echo " TIMEOUT"
        exit 1
    fi
done
echo " READY"
