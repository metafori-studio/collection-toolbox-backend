[server]
http_port = 3001
domain = localhost

[database]
type = sqlite3
path = grafana.db
# Disable WAL mode to avoid some disk I/O issues in restricted environments
wal = false

[users]
allow_sign_up = false
default_theme = dark

[auth.anonymous]
enabled = true
org_name = Main Org
org_role = Admin
